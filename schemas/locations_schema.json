{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Location Schema",
  "description": "Schema for location data files in Broken Divinity",
  "type": "object",
  "required": ["name", "initial_description", "explorable_items"],
  "properties": {
    "name": {
      "type": "string",
      "description": "Display name of the location"
    },
    "initial_description": {
      "type": "string",
      "description": "Description shown when player first enters location"
    },
    "explorable_items": {
      "type": "object",
      "description": "Items that can be examined in this location",
      "patternProperties": {
        "^[a-z_]+$": {
          "type": "object",
          "required": ["text", "description"],
          "properties": {
            "text": {
              "type": "string",
              "description": "Text shown in the menu option"
            },
            "description": {
              "type": "string",
              "description": "Detailed description when item is examined"
            }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
